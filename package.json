{
  "name": "begin-app",
  "version": "0.0.0",
  "description": "Begin basic Hello World! app for Node",
  "scripts": {
    "build": "eleventy",
    "watch": "eleventy --serve",
    "start": "run-s build && ARC_STATIC_SPA=false npx sandbox"
  },
  "devDependencies": {
    "@architect/sandbox": "latest",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@11ty/eleventy": "^0.11.0"
  }
}
